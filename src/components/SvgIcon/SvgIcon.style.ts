import { css } from '@style/css';

export const svgIconRawStyle = css.raw({
  color: '#333',
  aspectRatio: 1,
  width: '1rem',
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center'
});
