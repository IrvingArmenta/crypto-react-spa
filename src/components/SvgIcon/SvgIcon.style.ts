import { css } from '@style/css';

export const svgIconStyle = css({
  color: '#333',
  aspectRatio: 1,
  width: '1rem',
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  pointerEvents: 'none'
});
